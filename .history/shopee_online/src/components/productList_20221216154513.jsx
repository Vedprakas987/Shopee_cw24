import React from 'react';
import P
const ProductList = (products) => {
    return (
        <div>
            {products.map((el)=>(
                <Pt
            ))}
        </div>
    );
}

export default ProductList;
