import React from 'react';
import Prod
const ProductList = (products) => {
    return (
        <div>
            {products.map((el)=>(
                <Pt
            ))}
        </div>
    );
}

export default ProductList;
